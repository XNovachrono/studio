
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MOCK RULES FOR PRESENTATION: ALLOW ALL FOR LOCAL DEV EXPERIENCE
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
